<!doctype html>
<html lang = "en">
<body>
    <header>
        <a href ="/"> Home</a>
    </header>
 <main>
 {%if title %} <h1> {{title}} Hello !</h1> {% endif %}
 {%- if puppers%}
 <img src= "{{puppers}}" alt="a good doggy"/> 
 {%- endif %}
 {{content}}
</main> 
</body>
</html>